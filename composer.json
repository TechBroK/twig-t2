{
  "name": "techbrok/ticketflow",
  "description": "Professional ticket management system built with PHP and Twig",
  "require": {
    "twig/twig": "^3.0"
  },
  "autoload": {
    "psr-4": { "App\\": "src/" }
  },
  "scripts": {
    "serve": "php -S localhost:9000 -t src",
    "serve:prod": "php -S localhost:9000 -t src",
    "check": "php -l src/index.php && php -l src/bootstrap.php && php -l src/routes.php",
    "pre-deploy": "powershell -ExecutionPolicy Bypass -File scripts/pre-deploy.ps1",
    "deploy:docker": "powershell -ExecutionPolicy Bypass -File quick-deploy.ps1 -Environment docker",
    "deploy:render": "powershell -ExecutionPolicy Bypass -File quick-deploy.ps1 -Environment render",
    "deploy:local": "powershell -ExecutionPolicy Bypass -File quick-deploy.ps1 -Environment local",
    "post-deploy": "powershell -ExecutionPolicy Bypass -File scripts/post-deploy.ps1",
    "git:push": "git add . && git commit -m 'Deploy updates' && git push origin main",
    "deploy": "@deploy:render",
    "test": "@check"
  },
  "scripts-descriptions": {
    "serve": "Start local development server on port 9000",
    "check": "Check PHP syntax in core files",
    "pre-deploy": "Run pre-deployment checks and create backup",
    "deploy:docker": "Deploy using Docker",
    "deploy:render": "Deploy to Render.com",
    "deploy:local": "Test deployment locally",
    "post-deploy": "Run post-deployment verification",
    "test": "Alias for check command"
  }
}
